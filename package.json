{
  "name": "otp",
  "version": "2.0.0",
  "description": "OTP for OmniScript",
  "main": "app.js",
  "engines": {
    "node": "14.18.1"
  },
  "scripts": {
    "ngrok": "ngrok http 3000",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemailer": "^6.7.0"
  }
}
